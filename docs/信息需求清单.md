# 信息需求清单（统一汇总入口）

> 本文是"**项目还需要谁提供什么**"的唯一汇总表：每项写清 **用处、去向（哪个文件/config 键）、阻塞什么**。
> 拿到一项 → 填进"去向"一栏指的文件 → 状态改 ✅。缺值期间相关代码一律 `TODO(plant)` 保守拦截（铁律#5）。
> 各来源清单保留原处，本文只做聚合回链：PROGRESS §3 / 产品形态与安全 §4.C / 数据填写说明 §5 / INSTRUCTION_N1 拍板清单。
> 更新日期：2026-08-27（第 5 次：问师傅清单批次一回收 → A3/A4 转 🟡、新增 A12/B11）

## 🎯 当前硬缺口速览（按催办优先级）

1. **A3–A6 工艺约束真值**（原 docs/03 族：C2 厚度-时长 / C3 对流 / 梯度上限 / 炸板规则）——唯一让安全闸门"任何参数都放不了行"的原因
   （2026-08-27 批次一：A3/A4 转 🟡，拿到的是**行业通例不是本厂真值**，config 一格未填）
2. **A12 上风/下风到底指哪儿**——一句话能问清，但决定 39 列里那一栏的语义是否写错，**问清前别再基于它做判断**
3. **B11 每炉装载量**——装载率/单片面积直接驱动加热时长却没有栏位；顺带让实测能耗第一次可比（kWh → kWh/m²）
4. **B6 图像↔标注对齐约定**——不定则应力斑图像与 200 份标注对不上号，图白拍
5. **B1 F1 炉铭牌配置**——现场拍张照就能填，早填早受益
6. **A2 CCP 标定实验**——工厂选"参考最差样品"实测 Cmax/CPmax
7. **C1–C4 云端信息**——不急（文件包链路已闭环），但到位才能开联网层
8. **D3 诊断类目**——等维修+工艺座谈（前置：首批异常片自由文本）

## A. 安全与工艺真值（🔴 最高优先——不到位则安全闸门全拦，任何参数无法放行）

| # | 需要的文件/信息 | 用处 | 去向 | 阻塞什么 | 状态 |
|---|---|---|---|---|---|
| A1 | ~~docs/01 国标分级表~~ → **《钢化玻璃应力斑分级及检测方法》GB/T 草案已入库 docs/**（2026-07-02） | 质量判级的真值来源 | `config/grading.yaml` | — | ✅ 表1(X0.95)/表2(IsoT)/表3(CCP) 全部落 config；**草案版，正式版发布后逐值复核** |
| A2 | CCP 参考最差样品标定值（Cmax/CPmax） | CCP 归一化标定 | `config/ccp_reference.yaml` | CCP 只能出未标定值 | 🟡 公式与步骤已有权威出处（草案 §4.4：Ng=8、10000px²/100mm 归一、四向 GLCM）；**Cmax/CPmax 数值草案不给——需工厂选"参考最差样品"实测标定**（性质从等文档变为等标定实验） |
| A3 | **docs/03-hard-constraints.md**：C2 厚度→加热时长映射表 | 硬约束 C2 | `config/thresholds.yaml: thickness_duration` | constraints 全拦（无法判定→不通过） | 🟡 **仍不可填**。问师傅清单批次一（2026-08-27）拿到的是「一般单室上部对流炉」**产能表（炉次/小时，只到 8mm）**，换算得**炉次周期秒**——周期 ⊋ 加热，只是**上界**，且非本炉。有用副产品：**Low-E ≈ 白玻 +1/+2 厚度档**（可省一半实测量）。见《问师傅清单_答案登记》 |
| A4 | 同上：C3 对流风速区间 / 上下配比 | 硬约束 C3 | `config/thresholds.yaml: convection` | 同上 | 🟡 **仍不可填，且新增语义疑点**。批次一只给行业通例：热风机常用 Hz、风栅风压常用 %；上风>下风、按弓形方向调；下风「没有限制、特殊情况可到 0」（**与培训图「下部下限 80」冲突**）。无本炉读数、无比值、无照片。**先解 A12 再谈填值** |
| A5 | 同上：温度梯度上限（安全红线） | 防炸板 | `config/thresholds.yaml: safety.max_gradient` | 同上 | ⬜ |
| A6 | 同上：炸板风险判定规则（安全红线） | 防炸板 | `config/thresholds.yaml: safety.blowup_rule` | 缺规则保守置 blow_up_risk=True，全拦 | ⬜ |
| A7 | 同上：C4 / C5 定义（INSTRUCTION 提及但从未定义） | 补全约束体系 | `tools/constraints.py` + thresholds.yaml | 不臆造，未实现 | ⬜ |
| A8 | **docs/04-input-fields.md**：五类输入字段规格 | 输入 schema 定稿 | `schemas/inputs.py` | 输入层只有 ProcessParams 一类 | ⬜ |
| A9 | 二维分区邻接关系（当前 C1-c 按一维下标相邻简化） | C1-c 真实相邻判定 + 逆诊断"疑似哪个区" | thresholds.yaml 注释处 + constraints.py | 相邻温差检查失真；分区归因无从谈起 | ⬜ |
| A10 | ~~评估区域掩膜 8<t<10mm 边带分界~~ | 掩膜几何 | `tools/metrics.py::edge_band_mm` | — | ✅ 草案图3注明确：≤8mm 上限 200mm、≥10mm 上限 350mm、最小 50mm；公称厚度为离散档（6/8/10/12/15），不存在 8<t<10——现行实现与标准一致，消项 |
| A11 | **补偿模式下的 C1 限值口径**：元件损耗、邻区补偿场景允许的最大补偿温差/调温步长与适用条件（2026-07-03 现场确认：补偿实践**会超出** 5℃/3℃） | 让闸门不误拦被证明可行的补救，同时保住红线（口径到位前 C1 照拦不放宽） | `config/thresholds.yaml`（口径定了先改 config；如需"补偿模式"分支再动 constraints，过评审） | 补偿场景参数单恒 ⛔（当前保守合理）；三态诊断态②的判定阈值 | ⬜ 建议与 D3 座谈同场（见《设备带病运转与补偿_诊断口径》） |
| A12 | 🔴 **`convection_ratio_upper_lower` 的真实语义**：车间口语的「上风/下风」指**加热段炉内热风机的上下对流配比**，还是**淬冷段风栅的上下风压比**？两处各有无独立上下调节？ | 冻结的 39 列契约里这一栏到底记的是什么 | `schemas/process_params.py` 字段注释 + `tools/make_annotation_template.py::COLUMNS` 描述（若须改名则动契约，过评审） | **已回流数据的语义可信度**：若是后者，则该栏一直在记一个真实存在但语义完全不同的数，历史行须重标；AnnotationApp 现按「本炉无下部对流」把它标为 `na_fields` 灰显，前提也随之动摇 | ⬜ **一句话就能问清，最高优先**。三条线索指向疑点：①产能表标题「单室**上部**对流炉」→ 不存在上下对流配比；②培训图确认本炉无下部对流；③批次一第 4 题答的是「**风栅**上部风压大于下部」。见《问师傅清单_答案登记》第 4 题 |

## B. 炉体与现场（🔴 跨炉迁移的地基——每上一台新炉都要交一次）

| # | 需要的文件/信息 | 用处 | 去向 | 阻塞什么 | 状态 |
|---|---|---|---|---|---|
| B1 | **每台目标炉的铭牌照片 + 配置**：分区数、布局、风机数、型号/厂家 + **投产日期/上次大修日期**（炉龄特征 v2 的原料） | 炉体身份快照进每条样本（跨炉建模分组变量）+ 老化协变量 | `config/furnaces.yaml`——**用向导录入：`tools/furnace_setup.py`**（问答式，回车跳过=缺值） | furnace_config 全空；炉龄特征全按缺失 | ⬜ F1 待填 |
| B2 | 各炉的基准配方库（每个厚度/品类/模式桶的现行基准参数） | Δ=最终−基准 的基准侧；对话壳 --baseline | 现场配方文件 → 标注表 baseline_* 列预填 | 标注表 baseline 列靠手抄，易错 | ⬜ |
| B3 | 扫描仪型号/输出格式/接口（应力斑图像来源） | 图像接入 + ImageRef 尺寸/尺度字段 | 待建适配器 + `schemas/archive.py ImageRef` | 标注表只有图路径列，无尺寸/尺度 → 图像引用暂置空 | 🟡 已见 5 张样例生图（8bit 灰度、一图多片）；B6–B8 为看图后新增 |
| B4 | PLC/MES 取数接口（工况、能耗 kWh、出炉实测） | outcome 回填自动化 | 待建适配器 | measured_* 全靠手填 | ⬜ |
| B5 | 老师傅标注表（进行中，目标 200 份，深覆盖 4–6 个高产量桶） | 参数头训练数据 | `tools/ingest_annotations.py --furnace-id F1` 导入 `data/archive/` | 真训练（数据门 200 份） | 🟡 进行中 |
| B6 | **图像↔标注对齐约定**：文件名或旁车清单带 炉次号+片位（当前生图无任何可关联标识） | 切割后的单片图对上标注表 sample_id | 图像采集规范（与检测软件方定） | **不定则图像与 200 份标注对不上号，图白拍**（最急） | ⬜ 谈判筹码：草案 §7.1 本就强制检测报告含厚度/尺寸/日期时间——"未知/0MM/时钟错"是设备方不达标 |
| B7 | 检测软件端补齐文件名元数据：品类/厚度（当前"未知_未知_0MM"）+ 设备时钟校准（样例日期与实际差 8 个月，时间序切分/审计靠它） | 分桶键与判级依赖厚度；时间戳可信 | 检测软件配置 | 图像元数据不可用 | ⬜ |
| B8 | 定量光程差(nm)图可否导出（样例是 8bit 强度图，重斑片高光已饱和/削顶）；若可，固定曝光/增益或标注成像参数 | X0.95/IsoT/CCP 国标指标要 nm 图才能算 | 检测软件方确认 | 不能导出则国标三指标只能人工测，图像仅服务分布打分 | ⬜ **已有验收口径**（草案 §6.2.2/附录E）：分辨率 ≥0.5px/mm、光程差最大允差 ±10nm、重复性 ±5%、四向验证+三档标准片——直接拿这个问设备方 |
| B9 | 参数**下发通道与报文格式**（PLC 写入？人工抄单确认？） | 建议从"给人看"走向"可执行" | 与 B4 接口一起拍板 | 人读输出**已定并实现**（2026-07-02）：参数=Excel 参数单（`tools/param_sheet.py`，对话壳「导出参数单」同款），建议=中文文本；仅剩机器下发通道待定 | ⬜ 等 B4 |
| B10 | **《元件问题登记表》回收**（轻量：日期/时段/问题元件/是否邻区补偿/何时恢复） | 参数头训练去混杂（分清工艺调参 vs 设备补偿，遗漏变量偏倚）+ 补偿幅度=元件健康免费观测 | `docs/元件问题登记表.md` → 回收后与标注表按日期对齐存放 | 不记则补偿量被参数头当工艺规律学走；三态诊断态②无地面真值 | ⬜ 模板已建（2026-07-03），待发放 |
| B11 | **每炉装载量**：装载片数 / 单片尺寸(mm) / 装载面积(m²)，以及车间平时怎么记（片数？占炉床几成？） | ①去遗漏变量偏倚——批次一确认**装载率与单片面积直接驱动加热时长**，而 39 列没有这两栏，模型只能把这部分差异当噪声，学出来永远差一截且查不出原因；②`measured_energy_kwh` 是绝对 kWh，行业规格是 **kWh/m²**，没有面积则实测能耗**与任何规格都不可比**，E1 能耗节无法验证 | **先走 AnnotationApp `baseline_extra`**（自由键值，进 prompt/对比 CSV，**不进 39 列、不过闸门**），建议键 `装载片数` / `单片尺寸_mm` / `装载面积_m2`；攒到证据再论证是否扩列 | 参数头的加热时长残差长期偏大且不可解释；E1 能耗系数无法校验 | ⬜ **D5 曾拍板「39 列已够重，等证据再扩列」——这就是那个证据**（两条互不相关的线索同时指向它）。见《问师傅清单_答案登记》第 2 题 |

## C. 云与网络（🟡 多炉共享/自我迭代的联网层——文件包链路已可用，不联网不阻塞）

| # | 需要的信息 | 用处 | 去向 | 阻塞什么 | 状态 |
|---|---|---|---|---|---|
| C1 | 云端服务商与形态（自建机房 / 阿里云 / 华为云 / …） | 中心汇聚端选型 | `config/sync.yaml: cloud.provider` | 仅阻塞 HTTP 自动上传；文件包（U盘/内网共享 `drop_dir`）已可完整跑通 | ⬜ |
| C2 | 云端上传地址 | CloudTransport 适配器开发 | `config/sync.yaml: cloud.endpoint` | 同上 | ⬜ |
| C3 | 鉴权方式与密钥托管方案（密钥**绝不进代码/git**） | 同上 | `config/sync.yaml: cloud.auth_ref` | 同上 | ⬜ |
| C4 | 厂内网络策略：能否出网、出口白名单、IT 审批流程 | 部署可行性 | 部署文档 | 同上 | ⬜ |
| C5 | 数据出厂字段范围 | 机密边界定稿 | `docs/同步与自我迭代.md` §5 | — | ✅ **已定（2026-07-02）：全量含自由文本**（rationale/工况备注随包上自家受控云——诊断类目专项要靠它们；前提不变：仅自有通道+自家云） |
| C6 | 各炉本机标识分配 | 数据身份 | `config/sync.yaml: furnace_id` + furnaces.yaml | 多炉上线时才需要 | 🟡 命名规则已定（D6：F1/F2 顺序号，首炉=F1）；剩部署新机时逐台填 sync.yaml |

> 说明：C1–C4 到位并通过安全评审后，才开发联网适配器并同步调整 `config/review_rules.yaml`
> 的联网黑名单（当前黑名单全项生效——代码里出现联网库即被 review 硬拒，这是有意的）。

## D. 组织拍板（2026-07-02 过了一轮，能定的已定）

| # | 待拍板 | 用处 | 阻塞什么 | 状态 |
|---|---|---|---|---|
| D1 | Teacher LLM 选型 | N1 冷启动质量 | N1 本体开工 | ✅ **已定且已实施**：Qwen2.5-14B-Instruct 4bit GPU——权重已落盘实测跑通（加载≈30s/显存≈10GB，VERSION.md 登记；transformers 锁 `<5`，见 以往错误.md §5）；GGUF 3B 保留做 review/轻角色 |
| D2 | 复核工具形态 | 老师傅复核界面 | N1 前置#4 | ✅ **已定：Excel/CSV 表流（标注表同款介质）+ CLI 对话壳辅助**；Web/HMI 等 N1 终态再议 |
| D3 | **诊断类目专项**：异常/故障/失调分类 + 疑似分区（需维修+工艺共同敲定） | 逆诊断标签空间；对话壳诊断从"只报数"升级"下结论" | 归因头标签、标注表结构化诊断列 | ⬜ 类目待现场；**流程已定**：异常片自由文本聚类→带聚类结果与维修/工艺座谈→定类目→加标注列；**座谈底稿已备**：三态口径（健康/带病已补偿/带病未补偿，《设备带病运转与补偿_诊断口径》§2），同场顺带敲 A11 |
| D4 | zone_temps 是否进 Δ | Δ 契约定稿（改 DELTA_FIELDS 一处） | 参数头维度冻结 | ✅ **判据已预注册**（防事后选规则）：前 30 条中 zone_temps 有改动的样本占比 ≥20% → 纳入；<20% → 沿用基准。阈值在 `config/training.yaml: delta_review` |
| D5 | 模块级标注 | 标注表扩展 | — | ✅ **已定：不加**——39 列已够重，等一致性/覆盖数据给出证据再扩列 |
| D6 | 炉子命名约定 | 数据身份 | — | ✅ **已定：顺序号 F1/F2/…**，铭牌细节进 furnaces.yaml；将来多厂区再升级 `厂区-炉号` |

> fringe 打分侧（验收口径、top_fraction 经验值等）**不纳入本清单**——该功能独立管理，
> agent 收到的数据已带评好的分数（`MetricRecord.fringe_score_0_100`）；开放问题见《应力斑分布打分_需求与设计》§7。

## E. 研判输出层真值（🟡 2026-07-02 增，源=架构讨论稿 §4.2 六项输出对照；见《研判输出层_设计》）

> advisor/ 已按"结构先行"建好六项输出的出口：下列真值填进对应 yaml **即点亮该节**（无需改代码）；
> 填入前对应节输出"无法判定"并列明本表编号。

| # | 需要的文件/信息 | 用处 | 去向 | 阻塞什么 | 状态 |
|---|---|---|---|---|---|
| E1 | 能耗代理系数（参考温度/每区每℃功率/风机系数/基础功率）——现场电表或 PLC 实测回归 | 当前参数能耗估计（§4.2-5） | `config/energy.yaml` | 能耗节无法判定；最低能耗方案另需 A3-A6+质量模型 | ⬜ |
| E2 | 部件额定寿命（热电偶/风机/辊道/传动/风嘴）+ 老化三权重 + 维保阈值——设备厂家手册+维修班组 | 部件老化度与检修计划（§4.2-6） | `config/maintenance.yaml` | 维保节无法判定（运行数据另见 B4） | ⬜ |
| E3 | 炉床有效尺寸（长×宽 mm）+ 工艺片间最小间距 | 摆炉排布建议（§4.2-4） | `config/loading.yaml` | 摆炉节无法判定 | ⬜ |
| E4 | 缺陷→工艺原因映射表（信号/阈值/原因行，工艺专项敲定；与 D3 诊断类目同一次座谈） | 归因输出（§3.1.2；闸门 violations 已无条件透传不受阻） | `config/attribution.yaml: rules` | 归因节无法判定；分区定位另需 A9 | ⬜ |
| E5 | 新字段语义：对流风温（℃）取值范围与约束、风机启动逻辑的结构化形态 | ParamSet 可选字段转正 + 约束规则 | `schemas/process_params.py` 注释 + `config/thresholds.yaml` | 字段已建（可选、None 不拦），无规则校验 | ⬜ |

## 使用方式
1. 拿到任何一项 → 按"去向"列填入对应文件（config 改完即时生效，无需重启）。
2. 涉及安全限值（板块 A）→ 填完跑 `pytest tests/` 确认闸门行为变化符合预期。
3. 新炉上线 → 板块 B 全套走一遍 + `config/furnaces.yaml` 加条目 + 分配 furnace_id。
4. 已消项保留划线原文不删行——留痕"曾经缺过什么、靠什么解决的"。

## 变更记录
- 2026-07-02 ①初版四板块（P4 文档三件套）
- 2026-07-02 ②拍板批次二：D1/D2/D4/D5/D6/C5 落定；D1 随后实施（14B 落盘实测）
- 2026-07-02 ③看 5 张生图 → 新增 B6–B8；国标草案入库 → A1/A10 ✅、A2 转标定实验、B8 附验收口径
- 2026-07-02 ④初始化向导上线（B1 改向导录入+补日期字段）；输出格式拍板（参数=Excel/建议=中文）→ 新增 B9（仅剩下发通道）
- 2026-07-02 ⑤架构讨论稿 §4.2 输出对照 → 新增板块 E（能耗/维保/摆炉/归因/新字段五项真值），advisor 研判输出层结构先行落地
- 2026-07-03 ⑥带病运转+补偿现场知识落地 → 新增 A11（补偿模式 C1 口径）/B10（元件问题登记表）、D3 挂三态座谈底稿
- 2026-08-27 ⑦**问师傅清单批次一回收**（第 1~5 题，5 张图）→ A3/A4 转 🟡（**答的是行业通例、不是本厂真值，config 零改动**）；
  新增 A12（上风/下风语义存疑，🔴 最高优先）、B11（每炉装载量，两条独立证据同时指向）。
  逐题登记与 9 条追问见《问师傅清单_答案登记》；正文见《设备带病运转与补偿_诊断口径》
