# ============================================================
# Glass Agents — 训练环境依赖
# Python 3.12 venv | PyTorch cu128 (Blackwell RTX 5070)
# torch/torchvision/torchaudio 单独从 cu128 源安装，不在此文件
# ============================================================

# ---- 通用深度学习 ----
numpy
pandas
scipy
scikit-learn
matplotlib
seaborn
tqdm
jupyterlab
ipykernel
tensorboard
wandb

# ---- LLM 生态 (HuggingFace) ----
# transformers 锁 <5：5.12 v2 加载器在 Windows 上加载大模型（14B 分片）触发原生
# access violation（mmap 物化路径；分片哈希与官方一致已排除损坏），4.x 老路径实测正常。
# 详见 .claude/rules/以往错误.md §5。升 5.x 前须先用 14B 冒烟验证。
transformers<5
datasets
accelerate
peft
trl
bitsandbytes
sentencepiece
safetensors
tokenizers
evaluate
einops
huggingface_hub[hf_transfer]

# ---- 计算机视觉 ----
timm
opencv-python
albumentations
scikit-image
ultralytics
pycocotools

# ---- 测试 / 护栏 ----
pytest
pyyaml
pydantic            # M3 数据层 schema 校验

# ---- 代码质量（dev） ----
ruff                # 命名/风格 lint（pep8-naming N）
mypy                # 类型检查（宽松起步）

# ---- 标注表生成 ----
openpyxl            # 生成老师傅标注表 xlsx（枚举下拉校验）

# ---- 本地 LLM 推理（Teacher / 翻译官 / 知识库 / review）----
# llama-cpp-python 需从预编译 CPU 源装（Blackwell 无匹配 CUDA wheel）：
#   pip install "llama-cpp-python==0.3.30" --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
python-docx         # 简明说明/检测报告的 Word 版生成（make_docx_versions.py）
