# 硬约束阈值
# 真值来源：docs/03-hard-constraints.md（暂缺）。仅 C1 梯度温控有 INSTRUCTION 内联明确值；
# 其余 TODO(plant)。铁律：值为 TODO(plant) 的项，校验一律判"无法判定 → 不通过"，绝不放行。

gradient:                          # C1 梯度温控
  adjacent_zone_max_delta_c: 5     # 相邻分区温差 ≤5℃
  single_step_max_delta_c: 3       # 单次调温 ≤±3℃
  # TODO(plant): 二维分区邻接关系（当前实现用一维索引相邻简化）

thickness_duration: TODO(plant)    # C2 厚度→[时长下限, 上限]（s）
convection: TODO(plant)            # C3 品类→风速区间；上下对流配比范围

safety:
  blowup_rule: TODO(plant)         # 炸板风险判定条件
  max_gradient: TODO(plant)        # 温度梯度上限（安全红线）
