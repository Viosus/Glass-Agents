# CCP（方法 4.4）参考最差样品标定值
# 真值来源：现场标定（暂缺）。未标定前 ccp() 无法计算，相关测试 skip。

c_max: TODO(plant)     # 参考最差样品对比度 Cmax
cp_max: TODO(plant)    # 参考最差样品聚类突出 CPmax

# ---- W_w（提案指标：位置加权聚类突出指数，tools/metrics.texture_w —— 非现行草案条文） ----
# W_w 为零标定常数指标（归一化用纯数学上界 (2(Ng−1))⁴/12，无参考样耦合）；
# 本段仅存其唯一可选物理守卫 ε（仪器精度性质，非参考样统计量）。
# （前身 CCP_pos 及其 pos_c_max/pos_cp_max 参考常数已于 2026-07-30 整体废弃。）
min_dynamic_range_nm: TODO(plant)  # 最小动态范围条款 ε（nm）：标准化后 max−min < ε 判"无纹理"
                                   # 记 0（最优方向），堵 min/max 量化对"微幅平滑渐变"的放大。
                                   # 定值依据=光弹扫描仪测量精度水位（ASTM C1901 校验档位量级，
                                   # 建议 ±10nm 允差的 0.5~1 倍）；未标定 → 条款不激活（等价 ε=0）
