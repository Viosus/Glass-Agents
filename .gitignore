# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# ---- 虚拟环境 ----
.venv/

# ---- 编辑器 ----
.vscode/

# ---- Claude Code 本地态（不入版本库）----
.claude/settings.local.json
.claude/agent-memory/

# ---- 训练 / 运行期产物 ----
wandb/
runs/
data/archive/          # 归档样本（运行期落库，不入版本库）
*.ckpt
*.pt

# ---- 同步数据包（含配方=工厂机密：只经工厂自有通道上自家云，绝不入 git）----
data/outbox/
data/inbox/

# ---- 标注数据（填好的表=工厂配方，商业机密，绝不入库；仅保留模板）----
data/annotation/*
!data/annotation/标注表模板.csv
!data/annotation/标注表模板.xlsx

# ---- 本地大模型权重（不入版本库，元数据见 VERSION.md）----
models/
*.gguf

# ---- fringe_scoring 本地调参库（真实裁切图+逐片分数，仅本机完善模块用，不入库）----
fringe_scoring/local_store/

# ---- sample26 裁切核对导出（人工核对产物，export_sample26_* 可重生成，不入库）----
data/derived/sample26_thickness/warped_crops/
